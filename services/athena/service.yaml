apiVersion: v1
kind: Service
metadata:
  name: athena
  namespace: microservices
  labels:
    app: athena
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: athena
