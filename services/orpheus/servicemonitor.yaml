apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: orpheus
  namespace: microservices
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: orpheus
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
  namespaceSelector:
    matchNames:
    - microservices
