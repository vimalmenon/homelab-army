apiVersion: v1
kind: Service
metadata:
  name: orpheus
  namespace: microservices
  labels:
    app: orpheus
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: orpheus
