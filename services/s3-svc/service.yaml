apiVersion: v1
kind: Service
metadata:
  name: s3-svc
  namespace: microservices
  labels:
    app: s3-svc
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: s3-svc
