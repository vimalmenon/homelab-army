apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: hermes
  namespace: microservices
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: hermes
  endpoints:
  - port: http
    path: /metrics
    interval: 15s
  namespaceSelector:
    matchNames:
    - microservices
