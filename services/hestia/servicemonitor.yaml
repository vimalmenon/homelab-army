apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: hestia
  namespace: microservices
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: hestia
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
  namespaceSelector:
    matchNames:
    - microservices
