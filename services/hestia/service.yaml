apiVersion: v1
kind: Service
metadata:
  name: hestia
  namespace: microservices
  labels:
    app: hestia
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: hestia
