apiVersion: v1
kind: Service
metadata:
  name: clio
  namespace: microservices
  labels:
    app: clio
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: clio
