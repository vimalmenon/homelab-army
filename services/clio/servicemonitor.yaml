apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: clio
  namespace: microservices
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: clio
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
  namespaceSelector:
    matchNames:
    - microservices
