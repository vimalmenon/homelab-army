apiVersion: v1
kind: Service
metadata:
  name: hermes-pi-node-exporter
  namespace: monitoring
  labels:
    app: hermes-pi
    release: prometheus
spec:
  ports:
  - name: metrics
    port: 9100
    protocol: TCP
    targetPort: 9100
  clusterIP: None
