apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: slides-content
  namespace: slides
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
  storageClassName: nfs-client
  volumeMode: Filesystem
