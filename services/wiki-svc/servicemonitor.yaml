apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: wiki-svc
  namespace: microservices
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: wiki-svc
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
  namespaceSelector:
    matchNames:
    - microservices
