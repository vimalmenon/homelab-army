apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pihole-config
  namespace: default
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: nfs-client
