apiVersion: v1
kind: Service
metadata:
  labels:
    app: netalertx
  name: netalertx
  namespace: netalertx
spec:
  selector:
    app: netalertx
  ports:
  - name: web
    port: 20211
    protocol: TCP
    targetPort: 20211
  type: ClusterIP
