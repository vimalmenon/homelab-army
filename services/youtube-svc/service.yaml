apiVersion: v1
kind: Service
metadata:
  name: youtube-svc
  namespace: microservices
  labels:
    app: youtube-svc
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: youtube-svc
