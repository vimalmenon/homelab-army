apiVersion: v1
kind: Service
metadata:
  name: messages-svc
  namespace: microservices
  labels:
    app: messages-svc
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: messages-svc
