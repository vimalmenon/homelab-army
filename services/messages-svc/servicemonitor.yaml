apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: messages-svc
  namespace: microservices
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: messages-svc
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
  namespaceSelector:
    matchNames:
    - microservices
