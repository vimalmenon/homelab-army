apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: dynamo-svc
  namespace: microservices
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: dynamo-svc
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
  namespaceSelector:
    matchNames:
    - microservices
