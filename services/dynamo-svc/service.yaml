apiVersion: v1
kind: Service
metadata:
  name: dynamo-svc
  namespace: microservices
  labels:
    app: dynamo-svc
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: dynamo-svc
