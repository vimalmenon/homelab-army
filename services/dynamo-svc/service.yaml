apiVersion: v1
kind: Service
metadata:
  name: dynamo-svc
  namespace: microservices
  labels:
    app: dynamo-svc
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.128.211
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: dynamo-svc
